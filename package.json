{
  "name": "nexus-hub",
  "version": "1.0.0",
  "description": "Nexus Hub - Unified Workspace",
  "private": true,
  "scripts": {
    "install:client": "cd client && npm install",
    "install:server": "cd server && npm install",
    "install-all": "npm run install:client && npm run install:server",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "build:client": "cd client && npm run build",
    "lint:client": "cd client && npm run lint",
    "build:with-sync": "node scripts/antigravity-sync.js start-task build && npm run build:client && node scripts/antigravity-sync.js complete-task build success",
    "test:with-sync": "node scripts/antigravity-sync.js start-task test && npm run test && node scripts/antigravity-sync.js complete-task test success",
    "deploy:with-sync": "node scripts/antigravity-sync.js start-task deploy && npm run deploy && node scripts/antigravity-sync.js complete-task deploy success",
    "webhook:start": "node scripts/antigravity-webhook.js",
    "sync:backend": "node scripts/sync-backend.js",

    "test:smoke": "npx playwright test",
    "test:api": "npx playwright test rbac sms-relay --project=chromium",
    "test:e2e": "npx playwright test smoke dashboard --project=chromium",
    "test:rbac": "npx playwright test rbac --project=chromium",
    "test:relay": "npx playwright test sms-relay --project=chromium",
    "test:report": "npx playwright show-report"
  },
  "dependencies": {
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "concurrently": "^8.2.2"
  }
}
